<Application x:Class="TourPlanner.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:TourPlanner"
             Startup="Application_Startup">
    <Application.Resources>
        <ResourceDictionary>
            <local:IoCContainerConfig x:Key="IoCConfig" />

            <SolidColorBrush x:Key="Background" Color="#090909" />
            <SolidColorBrush x:Key="DataBackground" Color="#111111" />
            <SolidColorBrush x:Key="FontColor" Color="#eeeeee" />
            
            <SolidColorBrush x:Key="Primary" Color="#672DC9" />
            <LinearGradientBrush x:Key="Primary Gradient">
                <GradientStop Color="#672DC9" Offset="0" />
                <GradientStop Color="#222222" Offset="0.9" />
            </LinearGradientBrush>

            <LinearGradientBrush x:Key="Primary Gradient Hover">
                <GradientStop Color="#3616d9" Offset="0" />
                <GradientStop Color="#222222" Offset="0.9" />
            </LinearGradientBrush>

            <LinearGradientBrush x:Key="Primary Gradient Active">
                <GradientStop Color="#3616d9" Offset="0" />
                <GradientStop Color="#555555" Offset="0.9" />
            </LinearGradientBrush>

            <SolidColorBrush x:Key="PrimaryActive" Color="#FF9361FF" />
            <FontFamily x:Key="FontFamily">Tahoma</FontFamily>

            <LinearGradientBrush x:Key="GridSplitter Gradient">
                <GradientStop Color="#222222" Offset="0.1" />
                <GradientStop Color="#672DC9" Offset="0.5" />
                <GradientStop Color="#222222" Offset="0.9" />
            </LinearGradientBrush>

            <LinearGradientBrush x:Key="GridSplitter Gradient Hover">
                <GradientStop Color="#222222" Offset="0.1" />
                <GradientStop Color="#3616d9" Offset="0.5" />
                <GradientStop Color="#222222" Offset="0.9" />
            </LinearGradientBrush>

            <Style TargetType="Label">
                <Setter Property="Foreground" Value="{StaticResource FontColor}" />
            </Style>
            <Style TargetType="TextBox">
                <Setter Property="Background" Value="{StaticResource Background}" />
                <Setter Property="Foreground" Value="{StaticResource FontColor}" />
                <Setter Property="FontFamily" Value="{StaticResource FontFamily}" />
                <Setter Property="FontSize" Value="20" />
                <Setter Property="BorderBrush" Value="{x:Null}" />

                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="TextBoxBase">
                            <Border Name="Border" CornerRadius="10" Padding="10" Background="{StaticResource DataBackground}">
                                <ScrollViewer Height="30" x:Name="PART_ContentHost" />
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter> 
            </Style>
            <Style TargetType="DataGrid">
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="Foreground" Value="#eeeeee" />
            </Style>
            <Style TargetType="DataGridColumnHeader">
                <Setter Property="Background" Value="Transparent" />
            </Style>
            <Style TargetType="DataGridRow">
                <Setter Property="Background" Value="Transparent" />
            </Style>
            <Style TargetType="DataGridRowHeader">
                <Setter Property="Background" Value="#111111" />
            </Style>

            <Style TargetType="Button">
                <Setter Property="Foreground" Value="{StaticResource FontColor}" />

                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border Name="Border" CornerRadius="10" Background="{StaticResource Primary Gradient}" Padding="10">
                                <Border.Effect>
                                    <DropShadowEffect />
                                </Border.Effect>
                                <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Center" />
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Border" Property="Background" Value="{StaticResource Primary Gradient Hover}" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style TargetType="GridSplitter">
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="GridSplitter">
                            <Rectangle Name="Rectangle" Fill="{StaticResource GridSplitter Gradient}" />
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Rectangle" Property="Fill" Value="{StaticResource GridSplitter Gradient Hover}" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
